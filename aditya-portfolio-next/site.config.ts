/**
 * Published URL prefix on GitHub Pages (must match the repo name).
 * Wired into production builds via env — see `next.config.ts` and `npm run deploy`.
 */
export const publishedSiteBasePath = '/PersonalSite'
